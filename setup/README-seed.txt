The three *_seed.csv files in this folder contain live prospect data:
961 named business owners with direct phone numbers and email addresses.

They are listed in .gitignore and MUST NOT be committed. Upload them to
Supabase through the dashboard, then keep them on your Mac only.

If you ever see them appear in `git status`, stop and fix .gitignore before
pushing.
